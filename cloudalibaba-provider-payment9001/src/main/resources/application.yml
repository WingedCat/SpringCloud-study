server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        # server-addr: localhost:8848 #配置Nacos地址
        # 集群版本换成nginx
        server-addr: 192.168.233.170:1111

management:
  endpoints:
    web:
      exposure:
        include: '*'